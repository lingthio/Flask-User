.. _UserManager__Settings:

UserManager settings
====================

Below is a complete list of configurable Flask-User settings.

Note: Customization happens on the ``UserManager`` class.
The `UserManager__Settings` class is mixed into the `UserManager` class,
allowing us to split the code and docs across several files.

.. autoclass:: flask_user.user_manager_settings.UserManager__Settings
    :no-undoc-members:

